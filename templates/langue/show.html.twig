{% extends 'base.html.twig' %}

{% block title %}Langue{% endblock %}

{% block body %}
    <h1>Langue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ langue.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ langue.titre }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ langue.niveau }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ langue.specialite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('langue_index') }}">back to list</a>

    <a href="{{ path('langue_edit', {'id': langue.id}) }}">edit</a>

    {{ include('langue/_delete_form.html.twig') }}
{% endblock %}
